# 设计说明 —— 四节点控制栏

## 一句话

把银行 App 的功能全部收敛到**四个「根部控制节点」**：观测、行动、配置、设置。用户永远知道自己在哪里，也永远知道下一步该去哪。

## 为什么是这四个

传统银行 App 的信息架构是「按业务切模块」：我的、信用卡、贷款、理财、生活缴费……每上一个业务就多一个入口，最后底部栏挤到五个以上，用户靠图标形状去猜。

这里换一种切法：按**用户与钱的关系**切。

| 节点 | 心智动词 | 回答的问题 | 内容 |
| --- | --- | --- | --- |
| 观测 | 看 | 我的钱现在是什么状态？ | 账户卡片、账单日志、账单图表、消费地图 |
| 行动 | 做 | 我要让钱发生什么变化？ | 付款码 / 收款码、扫码付、转账、资金调度、还款 |
| 配置 | 调 | 单个账户该按什么规则运行？ | 限额、功能开关、还款方式、冻结 |
| 设置 | 改 | 这个软件本身该怎么表现？ | 外观、通知、安全、设备、关于 |

这四件事覆盖了全部操作，且**互斥**：

- 只读的归「观测」，会改变资金的归「行动」
- 约束「某个账户」的归「配置」，约束「软件本身」的归「设置」

任何新功能都能落进这四个格子，不需要再加第五个入口。这是这个 demo 想验证的核心命题。

## 四节点之间是连通的，不是四个孤岛

这是设计里最容易被忽略、但最能体现「一个系统」的部分：

- 在**配置**页关掉「免密支付」→ **观测**页的账单日志顶部立刻出现一条 `配置 / 星轨白金卡 · 免密支付 → 已关闭 / 已生效`
- 在**行动**页完成一笔资金调度 → **观测**页的日志出现转出与入账两条，并且**账户卡片上的可用额度当场变化**
- 在**观测**页点任意账户卡片 → 进入该账户详情，详情底部有「配置此账户」直达**配置**页并预选该账户

配置变更和资金变动共用同一条事件流。用户不需要理解「哪个页面归哪个模块」，只需要看那条一直在滚动的日志。

## 与常见银行 App 的对照

| 维度 | 传统银行 App | 本设计 |
| --- | --- | --- |
| 视觉基调 | 品牌色渐变 + 大圆角白卡 + 插画 | 近黑底 + 发丝级描边 + 无插画 |
| 主色用法 | 大面积铺底 | 只用于强调：指标、状态、指示条 |
| 金额 | 红色=支出、绿色=收入 | 支出用中性色，只有**入账**才染绿；红色保留给风险 |
| 信息密度 | 一屏一个大卡片 | 一屏多组数据，日志行高 30px |
| 数字排版 | 普通比例数字，跳动时宽度会变 | 全部 `tabular-nums`，滚动时数字不抖 |
| 图表 | 折线 + 渐变 + 阴影 | 极简 SVG，柱/环/热力/地图四类，无装饰 |
| 危险操作 | 弹窗二次确认 | **长按 0.9 秒**确认，进度条扫过，松手即取消 |
| 状态表达 | 文字说明 | 状态点 / pill / 脉冲动画，一眼可辨 |
| 主题 | 固定浅色 | 深色为默认，浅色与跟随系统可切 |
| 品牌色 | 不可改 | 用户可换，8 个预设，深浅主题下自动校正明度 |

## 设计令牌

所有视觉常量集中在 `src/styles/tokens.css`，组件不写魔数。

**表面与文字（深色为默认面）**

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--bg` | `#0a0b0d` | 页面底色 |
| `--surface-1` | `#111316` | 卡片 |
| `--surface-2` | `#171a1f` | 卡片内的次级块、输入框 |
| `--surface-3` | `#1e2229` | 轨道、空槽 |
| `--line` / `--line-strong` | 白 7% / 13% | 发丝描边 |
| `--text-1/2/3` | `#f2f4f7` / `#a5adba` / `#6c7481` | 主 / 次 / 弱文字 |

**强调色是可变的**

强调色以 **HSL 分量**（`--accent-h/s/l`）而非固定 hex 存放，这样能推导出同色系的浅底、描边、辉光与前景色，且不必依赖 `color-mix()`：

```
--accent        填充色
--accent-soft   14% 透明，用于底色块
--accent-line   36% 透明，用于描边
--accent-glow   24% 透明，用于光晕
--accent-fg     自动选黑或白（按 WCAG 相对亮度算）
```

浅色主题下会自动压低填充明度、再压暗文字明度，避免白底上出现糊掉的高饱和色。

**动效**

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `--dur-1` | 140ms | 按下反馈 |
| `--dur-2` | 220ms | 开关、分段滑块 |
| `--dur-3` | 340ms | 屏幕入场、图表生长 |
| `--ease` | `cubic-bezier(.2,.8,.2,1)` | 常规 |
| `--ease-out` | `cubic-bezier(.16,1,.3,1)` | 入场 |

全部动效服从 `prefers-reduced-motion`，并且在设置里提供了「图表动效」总开关。

## 竖屏 WAP 的适配要点

- `viewport-fit=cover` + `env(safe-area-inset-*)`，底部栏自动避让 iPhone 手势条
- 高度用 `100dvh` 而非 `100vh`，避免地址栏伸缩时出现裁切
- 所有可点元素 ≥ 44px；`:active` 有缩放反馈；不依赖 hover
- 全局 `-webkit-tap-highlight-color: transparent`，反馈由自己给，不用系统的灰块
- 金额列固定宽度 + `white-space: nowrap`，长数字不会挤压商户名
- 所有弹性网格列一律写 `minmax(0, 1fr)`——这是移动端最常见的横向溢出成因
- 页面级横向滚动被禁用，只有账户卡片轮播、账户 chips 等明确的地方允许横滑

## 各页设计意图

### 观测

- **默认收拢为卡包**：所有卡片叠在一起、以不同角度露出边缘，第一眼先给「总体是什么状态」，而不是「我有哪几张卡」
  - 卡包正面是一张总览卡：净资产、储蓄/信用张数与金额、信用额度占用
  - 点按卡包才铺开成逐张卡片，铺开后顶部出现「收起卡包」（手机上没有 hover，收拢必须是显式动作）
- **收拢时下方只给总体趋势**：净资产 30 天走势 + 资产构成
  - 净资产序列 = 各储蓄账户日终余额 − 各信用卡当日已用额度，两者本来就是同一套坐标，可以直接相减
  - 趋势之外只留三行数字（30 天支出 / 入账 / 笔数）；流水、图表、地图统统收进铺开态，避免首屏信息过载
- **展开后一切都围绕「当前那张卡」**：轮播滑到哪张，下方的小结、流水、图表、地图就全部换成那张卡的数据
  - 卡到哪，作用域到哪——不需要额外的「账户切换器」，轮播本身就是切换器
  - 信用卡的图里没有「居住」（房租不在信用卡上），Ⅱ 类卡没有大额分类，这些差异会自己显现；条状图的刻度也跟着重新缩放，不会因为混算全账户而把单卡数据压成细线
  - 小结区做成一张带账户名的标头（`星轨白金卡 | 信用卡 · •••• 4218`），让「下面这些数字是这张卡的」有明确锚点
  - 切卡或切视角时内容会重新入场（`key` 变化触发），提示「换了一份数据」
- **账户卡片轮播**（铺开后）：卡片宽 84%，右侧露出下一张的边缘，暗示「可以滑」；配 4 个圆点指示器，当前点拉长成胶囊
  - 信用卡：强调色描边 + 一层斜向浅色渐变，区分于储蓄卡
  - Ⅱ类卡：**虚线边框**，视觉上直接表达「这是一张受限的卡」
  - 卡内自下而上：类型/尾号/状态 → 产品名 → 主数字（30px）→ 用量进度 → 关键日期 → 30 天走势迷你图
- **三视角切换**：流水 / 图表 / 地图。默认流水——因为「刚刚发生了什么」比「这个月的饼图」更常被查看
- **账单日志**：等宽时间戳 + 类型小标签 + 商户 + 右对齐金额，行高 30px。
  - 类型标签按语义着色：入账绿、退款蓝、转账与配置用强调色（配置项虚线边框）
  - 交易中显示琥珀色并带省略号，失败则整行加删除线
  - 新事件从顶部滑入并闪一次强调色底
  - **暂停按钮**：日志一直在滚，用户想看某一条时需要能停下来
- **每日支出图**：柱=当日支出，绿点=当日入账。
  - 刻意不用「净现金流双向柱」：发薪日的一笔大额入账会把日常消费压成看不见的细线，30 根柱子里 29 根接近 0。用支出做单一刻度后，分布立刻可读
- **支出分类环图**：分片之间留 2 单位缝隙；图例带百分比与金额；「其他」在真实分类里已存在时会合并，不会出现两条同名
- **支出热力**：6 周 × 7 天，点格子看当天金额；右侧直接给出有支出天数、日均、最高单日
- **消费地图**：手写粗粒度中国轮廓 + 城市光点 + 从常驻地北京发出的弧线
  - 轮廓与城市点**共用同一个投影函数**（`projection.ts`），所以两者永远对齐
  - 点半径 ∝ √消费额，最大约 7px；常驻地有呼吸脉冲
  - 标签用 HTML 绝对定位而非 SVG `text`，这样字号是真实像素，不随 viewBox 缩放

### 账户详情

点卡片进入。这张卡只是「入口」，完整账本在这里：6 格统计（1px 缝隙形成发丝网格）、近 12 个月走势、分期进度、直达配置。

### 行动

- 顶部是付款码 / 收款码两个大按钮——这是线下最高频的动作，值得占最大面积
- 四个操作方块：扫码付、转账、**资金调度**、信用卡还款
- **资金调度是本设计的差异点**：
  - 转出 / 转入两组账户 chips，中间一个可旋转的双向箭头
  - 默认从储蓄账户转出、转入信用卡——跨信用卡取现是不该被默认推荐的路径
  - 滑块上限 = min(转出方可用, 转入方可还)，所以「全部」永远是合法金额
  - 拖动时**实时预览**转出后余额与到账后可用额度，手续费与到账时间也一并给出
  - 确认按钮承载三种认证方式，见「一个按钮承载三种认证」
- **最近动态**可点击重放：把商户与金额写进转账草稿后再跳转，省掉重复输入

### 配置

- 顶部账户 chips 切换
- 限额管理：单笔 / 日累计 / 月累计三条滑块，每条下方都带当前用量进度
  - Ⅱ类卡受监管约束的部分会打上「监管上限」标签，且**滑块无法越过**这个值
- 功能开关：境外交易、免密支付、线上支付、自动还款（仅信用卡）、夜间锁卡、交易提醒
- 每次改动都会向观测页的日志广播一条事件——这是「四节点是一个系统」的证据
- 危险操作区用红色描边与红色按钮，冻结/解冻是可逆的

### 设置

按「外观 / 界面 / 通知 / 安全 / 关于」分组，每组是一张发丝描边的行列表。外观可进二级页换主题、强调色与密度；设备与登录可下线设备并回看登录记录；关于页可以一键重置演示数据（保留用户设置）。

## 一个按钮承载三种认证

三个动作页（转账、扫码付、资金调度）的确认按钮不是三个，而是同一个「按住」控件。

| 手势 | 认证方式 | 为什么是这个方向 |
| --- | --- | --- |
| 原地按住 0.7 秒 | 6 位数字密码 | 最保守、最熟悉的兜底路径，不动就是它 |
| 按住**向上**甩 | 面容 ID | 上滑 = 把手机举到面前 |
| 按住**向下**甩 | 安全密钥（FIDO2） | 下滑 = 把密钥贴到机身底部的充电口 |

方向的映射不是随意分配的，而是让手势复刻真实动作的方位：脸在手机上方，充电口在手机下方。用户不需要记规则，只要想「我平时怎么解锁」然后把手机往那个方向动一下。

几个实现要点：

- **原地展开**：密码键盘不是弹窗，而是 `.auth-panel` 以 `position: absolute; bottom: calc(100% + 10px)` 从按钮**正上方长出来**（面板下沿与按钮顶边相距 10px，实测在 393×852 下完全落在视口内），所以视线不会跳，手指也不用移动。
- **不动才计时**：位移超过 26px 就判定为「打算甩」，按住进度按 2 倍速回退，甩到 46px 触发对应方式。所以抖动手不会误开键盘，中途犹豫也能退回。
- **意图实时反馈**：达到 25px 时按钮上下两侧的方向提示会点亮（`data-aim` / `data-active`），未达到阈值前用户就知道自己会被判定成哪种方式。
- **取消**：面板外的遮罩（`.auth-scrim`）与面板内的「取消」都回到 idle；`Esc` 同样可以退出。
- **触屏必需**：触发按钮与遮罩都设 `touch-action: none`，否则按住时页面会跟着滚动。
- **无障碍**：控件是 `role="button"` + `tabIndex`，`Enter`/空格直接打开密码键盘（键盘用户不必模拟手势），`Esc` 取消。

两套动画都是手写 SVG/CSS，不引入任何图形资源：

- **面容 ID**：Apple 风格致敬——圆角方框用 `pathLength={1}` + `stroke-dashoffset` 从 0 画满，双眼先左右打量再回正，扫描相位结束后方框收缩回弹、眼睛与嘴变成对勾。纯 `stroke` + `transform` 关键帧。
- **安全密钥**：自绘「密钥黏到充电口」——一个手机轮廓，底部充电口开一条缝，三圈涟漪从缝里扩散，密钥从下方 15px 处以 `translateY` + `rotate(-7deg)` 上升，70% 处过冲回弹后锁死在口上，随后亮起对勾。视觉上就是「啪一下吸上去」。

**同一套动画还会各回显一份到硬件的物理位置上**：

- 感应框 → 屏幕**顶部正中**（Face ID 传感器在机身顶部）
- NFC 纹波 → 屏幕**底部正中**，纹波原点正好压在屏幕最底边上（充电口在机身底部）

回显不是第二套动画：图形组件（`FaceGlyph` / `NfcRipples`）和 CSS 关键帧与面板里那份完全共用，`phase` 也由 `AuthButton` 统一驱动，所以面板与两处回显永远同步。这样既不用把页面假装成一台设备（不做全屏接管、不画真实插头线缆），又让「手按在按钮上、但感应发生在真机上」这件事看得见。

演示环境里三条路径都是成功路径（任意 6 位数字即可通过），不做失败态。

## 已知取舍

- **不做后端**：所有数据在前端用确定性伪随机生成，刷新即重置；只有设置项持久化到 `localStorage`
- **浅色主题只对观测页逐项打磨**：令牌层是完整的，其余页面在浅色下不会错乱，但没有逐屏校准
- **二维码是伪图形**：由种子推导的模块矩阵，有定位角但不可扫描
- **不做 PWA**：没有 manifest 与 service worker，验证方式为桌面浏览器的设备模拟
- **统计口径**：还款属于账户间内部划转，不计入「支出」，避免与刷卡消费重复计算；还款同时记在储蓄卡（出账）与信用卡（入账）两边，否则合成的净资产走势会把还款当成凭空消失的钱
